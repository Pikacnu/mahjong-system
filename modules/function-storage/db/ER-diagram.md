# Function Storage — ER Diagram

:::info
this doc is generated by AI
:::

This file contains an English ER diagram for the `function_storage` schema.

```mermaid
erDiagram
    RESOURCE {
        int id PK "generatedAlwaysAsIdentity"
        string code "not null"
        string hash "unique, not null"
    }
    METHOD {
        int id PK
        string name "unique, not null"
        string source_type "enum: builtin|user"
        datetime updated_at "default now"
    }
    VERSIONS {
        int id PK
        int version "not null"
        int method_id FK
        int resource_id FK
        datetime created_at "default now"
        string resource_type "enum: function|modules"
    }
    DEPENDENCIES {
        int id PK
        int source_version_id FK
        int dependency_version_id FK
    }
    PLUGIN_DEFINITIONS {
        int id PK
        int version_id FK "unique"
        string default_store "json string, default '{}'"
        datetime created_at "default now"
        datetime updated_at "default now"
    }

    METHOD ||--o{ VERSIONS : has
    RESOURCE ||--o{ VERSIONS : referenced_by
    VERSIONS ||--o{ DEPENDENCIES : source
    VERSIONS ||--o{ DEPENDENCIES : dependency
    VERSIONS ||--|| PLUGIN_DEFINITIONS : has_definition
```

Notes:

- versions links to method and resource via foreign keys.
- dependencies models a semantic dependency: depends on a `method` plus a `version_constraint`.
- resolved_version_id can optionally pin to a concrete `versions.id` (cache).
- plugin_definitions is one-to-one with versions (unique version_id).
