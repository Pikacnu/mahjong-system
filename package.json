{
  "name": "mahjong-things",
  "scripts": {
    "gen:proto": "bun --filter proto generate",
    "cluster:create": "kind create cluster --name mahjong-system --config ./cluster/kind-config.yaml",
    "cluster:delete": "kind delete cluster --name mahjong-system"
  },
  "dependencies": {
    "acorn": "^8.16.0",
    "zod": "^4.3.6",
    "@grpc/grpc-js": "^1.14.0"
  },
  "private": true,
  "workspaces": [
    "modules/*"
  ]
}